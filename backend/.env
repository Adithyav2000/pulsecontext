
# Local development environment variables for the backend.
# WARNING: do not commit secrets to version control.
# DB_URL: PostgreSQL connect string used by `settings.py` and `db.get_conn()`.
DB_URL=postgresql://pulse:pulse@localhost:5432/pulsecontext

# Default demo user ID used by `/seed` when no `user_id` is supplied.
USER_ID=adithya

